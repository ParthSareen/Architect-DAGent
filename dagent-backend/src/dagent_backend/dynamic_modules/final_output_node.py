def final_output(prev_output):
    # Assuming "clean up the result in English" means making it more readable or grammatically correct.
    # This solution is a simple placeholder as the actual implementation would depend on the specifics
    # of what needs to be cleaned up. For more complex cleaning, consider using regex, NLP libraries, etc.
    
    # Example cleanup
    cleaned_output = prev_output.strip().capitalize() + "."
    
    return cleaned_output